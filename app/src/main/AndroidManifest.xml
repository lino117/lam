<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <uses-permission
        android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Progettolam"
        tools:targetApi="31">


        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".recognitionTransition.UserActivityDetectionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.progettolam.transition.TRANSITIONS_RECEIVER_ACTION" />
            </intent-filter>
        </receiver>

        <receiver android:name="com.example.progettolam.autoRegistration.AutoRegistrationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.chronometer.AUTO_REGISTRATION" />
            </intent-filter>
        </receiver>

        <!--        <service-->
<!--            android:name="com.huawei.hms.location.ActivityIdentificationService"-->
<!--            android:exported="false"/>-->

        <service android:name="com.example.progettolam.specialFeature.StepCounterService"
            android:foregroundServiceType="health" />

        <service android:name="com.example.progettolam.chronometer.ChronometerService" />
    </application>

</manifest>